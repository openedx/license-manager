# Version constraints for pip-installation.
#
# This file doesn't install any packages. It specifies version constraints
# that will be applied if a package is needed.
#
# When pinning something here, please provide an explanation of why.  Ideally,
# link to other information that will help people in the future to remove the
# pin when possible.  Writing an issue against the offending project and
# linking to it here is good.

# Celery 5.0 has dropped support for python 3.5
celery<5.0

Django>=2.2,<2.3

edx-rest-api-client==1.9.2

# Inflect 4.0.0 requires python>=3.6
inflect<4.0.0

# mock >=4.0.0 drops support for python 3.5
mock<4.0.0

# path 13.2.0 drops support for Python 3.5
path<13.2.0

PyYAML>=5.1

redis==2.8

# Upgrade to 3.3.0 triggered users to lose superuser status; see ARCHBOM-1078 for details
social-auth-core==3.2.0

# social-auth-app-django>=3.2.0 requires social-auth-core>=3.3.0
social-auth-app-django==3.1.0

# stevedore>=2.0.0 requires python>=3.6
stevedore<2.0.0

zipp<2.0
